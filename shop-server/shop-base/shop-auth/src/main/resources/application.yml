server:
  port: 9001

spring:
  application:
    name: auth-service
  cloud:
    nacos:
      discovery:
        server-addr: nacos-server:8848


shop:
  encoder:
    crypt:
      secret: ${random.uuid}
      strength: 10